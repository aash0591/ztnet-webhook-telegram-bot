version: '3.8'

services:
  webhook-handler:
    image: aash591/ztnet-webhook-telegram-bot:latest  # Your Docker Hub image
    container_name: webhook-handler
    restart: unless-stopped
    environment:
      TELEGRAM_BOT_TOKEN: "7690659054:AAE9n3jZp_ZETXLiNWNVoOfx6RUICPeADow"  # Your actual bot token
      TELEGRAM_CHAT_IDS: "396661262,6153110527"  # Your chat ID
      WEBHOOK_URL: "http://webhook-handler:5000/webhook"  # Update if needed
    networks:
      - app-network
    ports:
      - "5000:5000"

networks:
  app-network:
    driver: bridge
